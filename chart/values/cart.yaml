metadata:
  name: cart
component: cart
labels:
  appName: cart
  project_name: expense
selector:
  appName: cart
  project_name: expense
initContainers:
  enabled: true
  imageVersion: 1.0.4
schema:
  schema_imageVersion: 1.0.1
  enabled: false
containers:
  appVersion: 1.0.1
  name: user
service:
  type: ClusterIP
ports:
  protocol: TCP
  port: 8080
  targetPort: 8080
ingress:
  enabled: false