metadata:
  name: frontend
component: frontend

initContainers:
  enabled: false

containers:
  appVersion: 1.0.2
  name: frontend

schema:
   enabled: false

service:
  type: ClusterIP

ports:
  protocol: TCP
  port: 80
  targetPort: 80

ingress:
  enabled: true
  host: frontend-dev.pdevops78.online